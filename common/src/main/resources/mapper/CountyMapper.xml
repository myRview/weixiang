<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hk.mapper.system.CountyMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hk.entity.system.CountyEntity">
        <id column="id" property="id" />
        <result column="county_name" property="countyName" />
        <result column="county_code" property="countyCode" />
        <result column="city_id" property="cityId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, county_name, county_code, city_id, create_time, update_time
    </sql>
    <delete id="deleteByCityId" parameterType="java.util.List">
        DELETE FROM sys_county
        WHERE city_id IN
        <foreach collection="list" item="cityId" open="(" separator="," close=")">
            #{cityId}
        </foreach>
    </delete>

</mapper>
